import React from 'react';
import PropTypes from 'prop-types';

function<%= helpers.capitalize(name) %> = {

    newFunction <%= helpers.capitalize(name) %>: () => {

    }
);


export default <%= helpers.capitalize(name) %>;
